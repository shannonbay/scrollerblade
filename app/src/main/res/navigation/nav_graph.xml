<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/SecondFragment">

   <!-- fragment
        android:id="@+id/StatsReviewFragment"
        android:name="com.github.shannonbay.wordstream.StatsReviewFragment"
        android:label="@string/stats_review_fragment_label"
        tools:layout="@layout/fragment_stats_review">

        <action
            android:id="@+id/action_FirstFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
    </fragment>
    <fragment
        android:id="@+id/SwapperFragment"
        android:name="com.github.shannonbay.wordstream.SwapperFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_swapper">

        <action
            android:id="@+id/action_FirstFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
    </fragment -->
    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.github.shannonbay.wordstream.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">

        <!-- action
            android:id="@+id/action_SecondFragment_to_FirstFragment"
            app:destination="@id/SwapperFragment" / -->
    </fragment>
</navigation>